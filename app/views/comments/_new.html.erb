	<script src="http://ajax.googleapis.com/ajax/libs/jquery/1.3/jquery.min.js" type="text/javascript"></script>
<script>
	jQuery(document).ready(function(){
		jQuery('#comment').focus(function() {
			jQuery(this).attr('value', '');
});

	});
</script>
<div class="comment_main">
	<div><h4>Post A Comment</h4></div>

	<%# form_for (:comment, :url => {:controller => 'comments',:action => 'create' }) do |f| %>
		<%#= f.error_messages %>
<% form_remote_tag :url=>{:controller => 'comments', :action => 'newcomment', :comment_type => @comment_type, :comment_id => @comment_id}, :html => {:action => {:controller => 'comments', :action => 'newcomment', :id => 'writecomment'}} do |f|%>
		  <!--<h4>Comment</h4>-->
		  	<div class="textarea"><%= text_area :comment,:description,:id => 'comment', :value => "Leave your comment here with as many characters you want." %></div>
		<p>
		  <%= submit_tag 'Post' %>
		</p>
	<% end %>
</div>
